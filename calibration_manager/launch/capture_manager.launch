<launch>

  <param name="robot_description" command="$(find xacro)/xacro.py '$(find r2_gazebo)/robots/r2c_full_body.sim.urdf.xacro'" />

  <node type="capture_manager.py"
        pkg="calibration_manager"
        name="calibration_manager"
        args="-c $(find calibration_manager)/example_capture_data/hardware_config -s $(find calibration_manager)/example_capture_data/hardware_config/system.yaml"
        output="screen">
  </node>

<!--
  <node type="urdf_pub.py" pkg="calibration_launch" name="urdf_pub"/>

  <node type="record" pkg="rosbag" name="robot_measurement_recorder"
        args="output-name /tmp/r2_calibration/cal_measurements robot_measurement robot_description">
     Hack to create the directory
    <param name="mkdir_tmp" command="mkdir -m 777 -p /tmp/r2_calibration" />
  </node>
-->

</launch>
